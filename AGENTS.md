# L2 MIND - Locked Decisions

- [2026-06-30] **Execution Model**: Headless CLI OpenRocket only. No GUI usage. (L2 MIND: Appearance vs Utility).
- [2026-06-30] **Optimization Algorithm**: Start with Brute Force Parameter Sweep. Move to Genetic Algorithms only if parameter space exceeds 100k combinations. (L2 MIND: Pain-driven abstraction, copy 3x abstract on 4th).
- [2026-06-30] **Resource Dependency**: Use local Python and local Java installation. No cloud infrastructure needed initially. (L2 MIND: Resource Dependency Check).
- [2026-06-30] **Time Allocation**: Strictly outside the Golden Block. (L2 MIND: Priority hierarchy enforcement).
- [2026-07-01] ~~**Standard Workflow**: All rocket design/optimization work follows the `l2-hyper-evolution` project skill...~~ **SUPERSEDED 2026-07-04** — see below.
- [2026-07-04] **Standard Workflow**: All rocket design/optimization work follows the `l2-organic-evolution` project skill (`.agents/skills/l2-organic-evolution/SKILL.md` — the correct, actual path; earlier docs pointed at nonexistent `.Codex/skills/`/`.claude/skills/` locations, fixed here): AST topology generation (`rocket_ast.py`) → Rust batch physics scorer (`l2_engine/src/bin/ast_eval.rs`, JSON-in/JSON-out subprocess contract) → OpenRocket ground-truth polish (`l2_hyper`/`organic_loop.py --validate-openrocket`). OpenRocket is the authority; Rust is the proxy. The old `l2-hyper-evolution` pipeline (fixed 3-stage `DesignGenome` template, `l2_engine evolve`) was retired 2026-07-04 in favor of organic, dynamic topology on real OpenRocket-sourced motors/materials. `l2_engine` is now fully self-contained (`rocket-sim` ported natively into `l2_engine/src/sim_core/`).
- [2026-07-20] **No Manual Rocket Polishing**: New designs and parameter refinement must run through organic AST generation → Rust population evaluation → automated OpenRocket authority polish → saved/reopened verification. If the engine lacks a required topology, material, geometry variable, motor role, or telemetry objective, implement, test, and document engine support before continuing. Manual candidate probes are allowed only to diagnose a demonstrated engine/OpenRocket model error.
- [2026-07-26] **Platform Scope**: The completed OSIFOG entry is now a reference case. The repository's product is a local-first rocket generation/evolution platform with a domain-neutral engine and external authority/runtime adapters.
- [2026-07-26] **KSP Scope**: Kerbal Space Program integration targets KSP 1.12.5 only: stock parts first, then CKAN-managed KSP1 mods. KSP2 is explicitly out of scope and must not consume roadmap, compatibility, or implementation effort.
- [2026-07-26] **Repository Hygiene**: Builds, virtual environments, downloaded simulators, upstream source checkouts, run populations, telemetry, and CKG memory are local generated state. Use the security-gated `scripts/repo_hygiene.py`; never commit or manually preserve generated bulk as source.
- [2026-07-26] **Open-Source License**: The retained engine is GPL-3.0-or-later because it contains modified OpenRocket-derived implementation details. Preserve OpenRocket and rocket-sim notices and the data provenance map in every source distribution.
