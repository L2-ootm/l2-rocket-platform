[pytest]
pythonpath = .
testpaths = tests
norecursedirs = .git .agents tools artifacts designs docs legacy lib net OSIFOG target
markers =
    slow: reopens/resimulates a saved .ork through the OpenRocket JVM (slow)

