# L2 MIND - Locked Decisions

- [2026-06-30] **Execution Model**: Headless CLI OpenRocket only. No GUI usage. (L2 MIND: Appearance vs Utility).
- [2026-06-30] **Optimization Algorithm**: Start with Brute Force Parameter Sweep. Move to Genetic Algorithms only if parameter space exceeds 100k combinations. (L2 MIND: Pain-driven abstraction, copy 3x abstract on 4th).
- [2026-06-30] **Resource Dependency**: Use local Python and local Java installation. No cloud infrastructure needed initially. (L2 MIND: Resource Dependency Check).
- [2026-06-30] **Time Allocation**: Strictly outside the Golden Block. (L2 MIND: Priority hierarchy enforcement).
- [2026-07-01] ~~**Standard Workflow**: All rocket design/optimization work follows the `l2-hyper-evolution` project skill...~~ **SUPERSEDED 2026-07-04** — see below.
- [2026-07-04] **Standard Workflow**: All rocket design/optimization work follows the `l2-organic-evolution` project skill (`.agents/skills/l2-organic-evolution/SKILL.md`): AST topology generation (`rocket_ast.py`) → Rust batch physics scorer (`l2_engine/src/bin/ast_eval.rs`, JSON-in/JSON-out subprocess contract) → OpenRocket ground-truth polish (`l2_hyper`/`organic_loop.py --validate-openrocket`). OpenRocket is the authority; Rust is the proxy — same law as before, but the topology is now generated organically (arbitrary stage count, real motors/materials sourced directly from OpenRocket's own bundled database) instead of GA-tuned over a fixed 3-stage template. The old `l2-hyper-evolution` pipeline (`l2_engine evolve`, `DesignGenome`, hardcoded Kick/Sustainer/Booster template) was retired 2026-07-04: it had accumulated hardcoded/hand-transcribed data (wrong motor diameters, a motor-designation resolver that silently only worked for 1 of 34 real motors) that the organic engine's real-data-sourced, dynamic design avoids by construction. `l2_engine` is also now fully self-contained — `rocket-sim` was ported natively into `l2_engine/src/sim_core/`, no external base-repo dependency remains. `.agents/skills/l2-hyper-evolution/` is kept as a historical/retired reference, not the active workflow.
